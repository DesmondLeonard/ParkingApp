import { Slot } from "expo-router";

export default function ParkingLayout() {
  return <Slot />;
}

//At a minimum what we need in a Layout file. So basically we are creating new layout with (Parking), and here
// is the layout file.
